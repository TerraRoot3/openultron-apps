# openultron-apps

OpenUltron public catalog repository for sandbox web apps.

## Layout

- `apps/<app-id>/`: one app per directory
- each app directory must contain `manifest.json`

## Included Demo

- `apps/demo`: starter app for OpenUltron auto-sync tests
